# Future: Place trained model weights here (.tflite, .onnx)
# The risk_scorer.py module is designed with a strategy pattern so an
# MLScorer can be swapped in without changing the rest of the pipeline.
